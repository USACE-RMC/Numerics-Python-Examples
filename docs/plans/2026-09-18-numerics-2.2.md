# Numerics 2.2 and .NET 10 Examples Implementation Plan

## Goal and approved decisions

Update every example to the published RMC.Numerics 2.2.0 package and .NET 10.
Work on `numerics-2.2-examples`, based on `origin/main` at `725a1cd`, in
an isolated worktree. Preserve the seven dirty notebooks in the original
checkout. Keep source changes and commits local; main is protected.

Pin the package exactly, use inline C# and Roslyn compilation in notebook 05,
retain validated benchmark outputs, and perform a focused compatibility sweep
without adding a general tour of new library features.

## Global constraints

- Exact NuGet package: RMC.Numerics 2.2.0; assembly version: 2.2.0.0.
- Target .NET 10 through an explicit runtime configuration; require a .NET 10 SDK.
- Do not edit the Numerics repository or its algorithms, seeds, or tolerances.
- Scientific claims and speedup claims must follow measured results.
- Compile only the trusted example C# source, in memory; no committed binaries.
- Every notebook must validate and execute, as must all three example scripts.

## Task 1: Package, runtime, and setup

Add a net10.0 restore project with an exact PackageReference, local NuGet
configuration, and explicit CoreCLR runtime configuration. Centralize loading
through notebook helpers, preserving the resolve_numerics_dll interface and
validating the exact assembly and target runtime. All 13 notebooks and three
scripts must use the same loader. Keep NUMERICS_DLL as an explicit validated
development override. Document restore/install commands and .NET 10 prerequisites
in README, notebook 00, and Python requirements. Test missing/wrong packages,
override validation, actual CoreCLR version, and representative Numerics calls.

## Task 2: MCMC and compiled likelihood

Refresh notebooks 03-06 for current warmup, output, transition, acceptance,
rank-normalized split/folded R-hat, conservative multi-chain ESS, and NUTS mass
adaptation semantics. Keep chain boundaries in diagnostics and exclude warmup.
Explain the log-posterior delegate contract and when uniform priors contribute
only a constant. Python callbacks stay sequential.

Notebook 05 contains an inline C# Normal likelihood class with immutable copied
data and inline Roslyn compilation/binding. Bind a real managed delegate via
reflection, without wrapping the managed method in a Python callback. Explain
the analogy to RStan's compiled C++ model and the difference between Roslyn IL
compilation/JIT and Stan automatic differentiation.

Benchmark Python/sequential, C#/sequential, C#/parallel, and PyMC NUTS. All use
the same data and uniform bounds, four chains and seed 12345. Numerics uses
warmup 1000, iterations 2000, output length 4000, thinning 1; concatenate each
chain's 1000 post-warmup Markov draws and 1000 output draws. PyMC uses tune 1000,
draws 2000, four chains. Report 12000 total transitions, compile/setup separately,
median/range of three sampling runs, posterior means, R-hat, bulk/tail ESS,
conservative ESS/second, MCSE, and NUTS diagnostics. Use one reduced untimed
warm-up per mode. Verify likelihood parity at finite and invalid inputs and
managed serial/parallel reproducibility. Report measured scaling without
requiring a predetermined speed advantage for these small models.

## Task 3: Random Forest

Refresh notebook 11 contextual discussion and train/predict timings. Match
100 trees, depth 20, minimum split 2, seed 12345, sqrt(d) classifier features,
and all regressor features. Compare mean regression predictions (column 3) with
scikit-learn and identify the library's classification aggregation explicitly.
Use fresh models for one warm-up plus three timed repetitions. Record median
and range, scores and deterministic repeatability. Describe indexed tree growth,
pure-node termination, parallel execution, and limits of ensemble quantiles.

## Task 4: Compatibility, output policy, and validation

Add a narrow CONTRIBUTING exception for maintained release benchmark outputs.
Record Python, dependencies, OS/CPU/core count, .NET runtime, SDK/Roslyn, assembly
and package provenance. Execute notebooks in fresh kernels and scripts in
separate processes. Keep outputs for 00, 05, 06, 11; keep other execution results
as ignored validation artifacts. Repair affected baseline schema errors and
scientific exposition where needed. Store test and benchmark evidence in a
release report. Run helper tests, notebook schema checks, all examples, semantic
diff review and git diff --check. An independent final review checks scope,
scientific fairness, error handling and evidence before local commits.

## Acceptance and release gate

Restore the published package from NuGet.org into an empty project-local cache.
Do not reuse the old local 2.2 package. If NuGet publication is incomplete,
prepare code and validate available isolated artifacts, then report the exact
remaining official-package gate. For reference NUTS runs require R-hat < 1.01,
finite diagnostics and no unexplained divergences; compare means using pooled
MCSE. Failed statistical checks require diagnosis, not tuning library behavior.
Every completed claim must identify the commands and evidence supporting it.
